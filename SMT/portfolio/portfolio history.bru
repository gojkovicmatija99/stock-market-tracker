meta {
  name: portfolio history
  type: http
  seq: 4
}

get {
  url: http://localhost:8083/portfolio/history?interval=15min
  body: none
  auth: bearer
}

params:query {
  interval: 15min
}

auth:bearer {
  token: {{token}}
}
